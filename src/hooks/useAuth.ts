// Re-export the useAuth hook from the context
export { useAuth } from '@/context/AuthContext';

// You can add additional auth-related hooks here if needed
// For example, hooks for specific auth operations or utilities 